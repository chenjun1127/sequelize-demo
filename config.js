const config = {
    // 数据库配置
    database: {
        DATABASE: 'test_db',
        USERNAME: 'root',
        PASSWORD: 'cj123456',
        PORT: '3306',
        HOST: 'localhost'
    }
};

module.exports = config;